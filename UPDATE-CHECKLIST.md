# 🔄 版本更新检查清单

每次发布新版本前，请按照此清单操作。

## 📝 发布前准备

### 1. 更新版本号（必须）

**修改 VERSION.json：**
```json
{
  "version": "1.3.0",  // ← 更新这里
  "releaseDate": "2025-11-06",  // ← 更新日期
  "changes": [
    "添加了什么新功能",
    "修复了什么bug",
    "改进了什么"
  ]
}
```

**版本号规则（语义化版本）：**
```
主版本号.次版本号.修订号

例如：1.3.2
- 1：重大更改，可能不兼容旧版本
- 3：新功能，向后兼容
- 2：Bug 修复，向后兼容

常见情况：
- 修复 bug → 1.2.0 → 1.2.1
- 添加新功能 → 1.2.1 → 1.3.0
- 重大重构 → 1.3.0 → 2.0.0
```

**同步更新 package.json：**
```json
{
  "version": "1.3.0"  // ← 保持一致
}
```

---

### 2. 测试功能（建议）

```bash
# 启动应用测试
quick-start.bat

# 测试主要功能：
□ PDF 上传
□ AI 提取
□ Procore 自动化
□ 设置保存
```

---

### 3. 提交代码到 Git

```bash
# 添加所有更改
git add .

# 提交（使用有意义的消息）
git commit -m "v1.3.0: 添加新功能描述"

# 推送到 GitHub
git push
```

---

### 4. 在 GitHub 创建 Release

**访问：**
```
https://github.com/jundongGit/PO-workflow/releases/new
```

**填写表单：**

1. **Tag version**: `v1.3.0` （与 VERSION.json 一致）

2. **Release title**: `Version 1.3.0 - 简短描述`

3. **Description**: （从 VERSION.json 复制 changes）
   ```markdown
   ## 🎉 版本 1.3.0

   ### ✨ 新功能
   - 添加了什么新功能

   ### 🐛 Bug 修复
   - 修复了什么问题

   ### 💡 改进
   - 改进了什么体验

   ---

   ### 📦 更新方法
   远程用户请双击 `auto-update.bat` 自动更新
   ```

4. **点击 "Publish release"**

---

## ⚡ 快速命令（复制粘贴）

**完整更新流程（一次性执行）：**

```bash
# 1. 确认你已经更新了 VERSION.json 和 package.json

# 2. 添加并提交
git add .
git commit -m "v1.3.0: 你的更新说明"

# 3. 创建 tag
git tag v1.3.0

# 4. 推送代码和 tag
git push origin main
git push origin v1.3.0

# 5. 然后去 GitHub 创建 Release（使用上面的 tag）
```

---

## 🎯 版本更新示例

### 示例 1：Bug 修复

**修改前：** v1.2.0
**修改后：** v1.2.1

**VERSION.json：**
```json
{
  "version": "1.2.1",
  "releaseDate": "2025-11-06",
  "changes": [
    "修复了 PDF 上传失败的问题",
    "修复了 API Key 无法保存的 bug"
  ]
}
```

**Git 提交：**
```bash
git add .
git commit -m "v1.2.1: Fix PDF upload and API key saving bugs"
git tag v1.2.1
git push origin main
git push origin v1.2.1
```

---

### 示例 2：新功能

**修改前：** v1.2.1
**修改后：** v1.3.0

**VERSION.json：**
```json
{
  "version": "1.3.0",
  "releaseDate": "2025-11-10",
  "changes": [
    "添加批量上传功能",
    "支持导出 Excel 报告",
    "改进了 UI 界面"
  ]
}
```

**Git 提交：**
```bash
git add .
git commit -m "v1.3.0: Add batch upload and Excel export"
git tag v1.3.0
git push origin main
git push origin v1.3.0
```

---

### 示例 3：重大更新

**修改前：** v1.9.5
**修改后：** v2.0.0

**VERSION.json：**
```json
{
  "version": "2.0.0",
  "releaseDate": "2025-12-01",
  "changes": [
    "重新设计的用户界面",
    "数据库迁移到 PostgreSQL",
    "API 接口重大变更"
  ],
  "breakingChanges": [
    "需要重新配置数据库",
    "旧版本的设置文件不兼容"
  ],
  "requiresUpdate": true
}
```

---

## ⚠️ 注意事项

### ❌ 常见错误

1. **忘记更新 package.json 版本号**
   - VERSION.json: v1.3.0
   - package.json: v1.2.0 ❌
   - **结果**：版本号不一致

2. **Git tag 与 VERSION.json 不一致**
   - VERSION.json: v1.3.0
   - Git tag: v1.2.0 ❌
   - **结果**：自动更新会混乱

3. **忘记推送 tag**
   ```bash
   git push origin main  ✅
   git push origin v1.3.0  ❌ 忘记了
   ```
   - **结果**：GitHub Release 找不到 tag

### ✅ 最佳实践

1. **保持版本号一致**
   - VERSION.json ✅
   - package.json ✅
   - Git tag ✅
   - Release tag ✅

2. **有意义的提交消息**
   ```bash
   ❌ git commit -m "update"
   ✅ git commit -m "v1.3.0: Add batch upload feature"
   ```

3. **发布前测试**
   - 本地运行测试
   - 检查主要功能
   - 确认没有错误

4. **清晰的更新日志**
   ```markdown
   ❌ "修复了一些问题"
   ✅ "修复了 PDF 上传时文件名包含特殊字符导致失败的问题"
   ```

---

## 📊 版本历史示例

```
v2.0.0 - 2025-12-01 - 重大更新：新UI、新数据库
v1.9.5 - 2025-11-25 - 修复：性能优化
v1.9.0 - 2025-11-20 - 新功能：批量导出
v1.8.1 - 2025-11-15 - 修复：API 连接问题
v1.8.0 - 2025-11-10 - 新功能：自动更新系统
v1.7.0 - 2025-11-05 - 初始版本
```

---

## 🔍 如何检查当前版本？

**方法 1：查看文件**
```bash
# 查看 VERSION.json
type VERSION.json

# 查看 package.json
type package.json
```

**方法 2：通过 API**
```bash
curl http://localhost:3001/api/version
```

**方法 3：通过 Git**
```bash
git describe --tags
# 输出：v1.2.0
```

---

## 🆘 常见问题

### Q: 如果发布后发现错误怎么办？

**A: 立即发布修复版本**
```bash
# 修复问题
# 更新版本号：1.3.0 → 1.3.1
git add .
git commit -m "v1.3.1: Hotfix - 修复紧急问题"
git tag v1.3.1
git push origin main
git push origin v1.3.1
# 创建新的 Release
```

### Q: 可以删除已发布的 Release 吗？

**A: 可以，但不推荐**
- 如果用户已经下载了，可能会困惑
- 最好发布一个修复版本

### Q: 版本号写错了怎么办？

**A: 删除错误的 tag 并重新创建**
```bash
# 删除本地 tag
git tag -d v1.3.0

# 删除远程 tag
git push origin :refs/tags/v1.3.0

# 创建正确的 tag
git tag v1.3.1
git push origin v1.3.1
```

### Q: 如何回退到旧版本？

**A: 远程用户可以下载指定版本的 Release**
- 访问：https://github.com/jundongGit/PO-workflow/releases
- 选择需要的版本
- 下载并安装

---

## 📚 相关资源

- [语义化版本规范](https://semver.org/lang/zh-CN/)
- [GitHub Release 文档](https://docs.github.com/cn/repositories/releasing-projects-on-github)
- [Git Tag 使用指南](https://git-scm.com/book/zh/v2)

---

**保存此清单，每次更新前检查一遍！** ✅
